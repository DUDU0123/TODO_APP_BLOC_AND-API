import 'package:flutter/material.dart';

class ShowTodoDetailsPage extends StatelessWidget {
  const ShowTodoDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Container()
        ],
      ),
    );
  }
}